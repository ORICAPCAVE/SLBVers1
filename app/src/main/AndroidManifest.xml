<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.2slbvers1"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".After_Logged_in"
            android:exported="true" />
        <activity
            android:name=".dedication_page_activity"
            android:exported="true"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity2"
            android:exported="true"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".Play_Bug"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout"
            android:exported="true" />
        <activity
            android:name=".Registration"
            android:exported="true"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".Request_Password"
            android:exported="true"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".rules_activity"
            android:exported="true" />
        <activity
            android:name=".rules_activity2"
            android:exported="true"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".slug_bub_reward"
            android:exported="true"
            android:configChanges="orientation|screenSize"/>

        <activity
            android:name=".SlugBugScore"
            android:exported="true"/>

    </application>

</manifest>
